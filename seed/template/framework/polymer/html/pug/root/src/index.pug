doctype html
html(lang='es')
    head
        meta(charset='utf-8')
        title <%= project %>
        meta(content='' name='description')
        //- link(href='favicon.ico' rel='shortcut icon')
        link(href='components.main.html' rel='import')

    body
        //< if (args.electron && args.dev) { >//
        script(async='' id='__bs_script__' src='//<= paths.electron.livereload >//')
        //< } >//

        ss-table
            .table
                .line
                    ss-box
                    ss-box
                    ss-box

                .line
                    ss-box
                    ss-box
                    ss-box

                .line
                    ss-box
                    ss-box
                    ss-box
