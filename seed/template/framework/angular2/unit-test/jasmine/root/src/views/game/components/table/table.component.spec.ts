import 'jasmine'

describe('Component: table', () => {
    let check: boolean

    beforeEach(() => {
        check = true
    })

    it('check test', () => {
        expect(check).toBe(true)
    })
})
