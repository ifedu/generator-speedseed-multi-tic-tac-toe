import 'jasmine'

describe('Component: user', () => {
    let check: boolean

    beforeEach(() => {
        check = true
    })

    it('check test', () => {
        expect(check).toBe(true)
    })
})
