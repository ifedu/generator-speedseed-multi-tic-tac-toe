import { assert } from 'chai'

describe('Component: config', () => {
    let check: boolean

    beforeEach(() => {
        check = true
    })

    it('check test', () => {
        assert.equal(check, true)
    })
})
