import 'jasmine'

describe('Component: game', () => {
    let check: boolean

    beforeEach(() => {
        check = true
    })

    it('check test', () => {
        expect(check).toBe(true)
    })
})
