import { assert } from 'chai'

describe('Component: login', () => {
    let check: boolean

    beforeEach(() => {
        check = true
    })

    it('check test', () => {
        assert.equal(check, true)
    })
})
