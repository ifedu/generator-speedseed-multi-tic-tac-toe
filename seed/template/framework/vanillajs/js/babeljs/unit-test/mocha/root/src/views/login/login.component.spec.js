import 'mocha'
import { assert } from 'chai'

describe('Component: login', () => {
    let check

    beforeEach(() => {
        check = true
    })

    it('check test', () => {
        assert.equal(check, true)
    })
})
