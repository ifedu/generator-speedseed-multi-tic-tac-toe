import { assert } from 'chai'

describe('Component: score', () => {
    let check

    beforeEach(() => {
        check = true
    })

    it('check test', () => {
        assert.equal(check, true)
    })
})
