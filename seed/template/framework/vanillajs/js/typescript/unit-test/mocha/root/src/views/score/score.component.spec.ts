import { assert } from 'chai'

describe('Component: score', () => {
    let check: boolean

    beforeEach(() => {
        check = true
    })

    it('check test', () => {
        assert.equal(check, true)
    })
})
