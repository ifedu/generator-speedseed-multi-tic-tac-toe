import 'jasmine'

describe('Component: config', () => {
    let check: boolean

    beforeEach(() => {
        check = true
    })

    it('check test', () => {
        expect(check).toBe(true)
    })
})
